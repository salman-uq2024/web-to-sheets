# Environment variables for web-to-sheets
# Copy this to .env and fill in your values. Never commit .env to version control.

# Google Sheets Configuration
GOOGLE_SHEETS_CREDENTIALS_PATH=path/to/your_service_account.json
GOOGLE_SHEETS_ID=your_google_sheet_id

# Logging
LOG_LEVEL=INFO

# Optional: Slack Notifications (for alerts on failures)
SLACK_WEBHOOK_URL=YOUR_SLACK_WEBHOOK_URL_HERE

# Optional: Basic Auth (if site requires it, configure in site YAML with env vars)
# BASIC_AUTH_USERNAME=your_username
# BASIC_AUTH_PASSWORD=your_password