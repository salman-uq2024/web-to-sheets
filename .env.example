# Optional environment variables for integrations. Duplicate this file to
# `.env` (which is ignored) and fill in real values when deploying.

# Google Sheets spreadsheet ID used by SheetsExporter. Leave unset to skip.
SHEET_ID=

# Incoming webhook URL for Slack alerts on failures. Leave unset to disable.
SLACK_WEBHOOK_URL=
