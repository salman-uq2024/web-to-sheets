# Core dependency definitions live in pyproject.toml. This file exists for
# compatibility with tooling that still expects a requirements.txt. Installing
# it will pull in the package in editable mode plus test tooling.
-e .
pytest==8.4.2
pytest-mock==3.15.1
ruff==0.6.4
